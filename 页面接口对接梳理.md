# BlueEdge 页面与官方 BFF 对接梳理

更新时间：2026-05-27

## 1. 当前结论

当前架构已经跑通：

```text
frontend -> Vite proxy -> official BFF /api/v1 -> Kubernetes / KubeEdge cluster
frontend -> api-gateway -> official BFF /api/v1 -> Kubernetes / KubeEdge cluster
```

已验证节点接口可从真实集群返回数据：

```text
GET /api/v1/node
```

当前真实节点数据包括：

```text
k8s-laptop-edge
k8s-master
k8s-worker01
```

需要注意：官方 BFF 当前实际接口前缀是：

```text
/api/v1
```

不是早期文档中写的：

```text
/api
```

## 2. 页面对接状态总表

| 模块 | 前端路由 | 前端页面 | 当前数据来源 | 官方 BFF 接口 | 当前状态 | 主要待办 |
| --- | --- | --- | --- | --- | --- | --- |
| 仪表板 | `/` | `Dashboard.tsx` | 部分真实 BFF 数据 | 可组合 `/node`、`/deployment`、`/device`、`/rule`，也可走 `api-gateway /overview` | 已接节点/部署概览 | CPU/内存趋势、事件仍需 metrics 或事件接口 |
| 节点 | `/nodes` | `Nodes.tsx` | 真实 BFF 数据 | `GET /api/v1/node`、`GET /api/v1/node/{name}`、`PUT /api/v1/node`、`DELETE /api/v1/node/{name}` | 已打通列表 | 补详情字段、删除/更新真实调用、字段展示优化 |
| 节点组 | `/nodegroups` | `NodeGroups.tsx` | 真实 BFF 数据 | `GET /api/v1/nodegroup`、`GET /api/v1/nodegroup/{name}`、`POST /api/v1/nodegroup`、`PUT /api/v1/nodegroup`、`DELETE /api/v1/nodegroup/{name}` | 已打通列表 | 补详情字段、创建/更新/删除真实调用 |
| 部署 | `/deployments` | `Deployments.tsx` | 真实 BFF 数据 | `GET /api/v1/deployment`、`GET /api/v1/deployment/{namespace}`、`GET /api/v1/deployment/{namespace}/{name}`、`POST /api/v1/deployment/{namespace}`、`PUT /api/v1/deployment/{namespace}`、`DELETE /api/v1/deployment/{namespace}/{name}` | 已打通列表 | 补详情字段、创建/伸缩/暂停/删除真实调用 |
| 边缘应用 | `/edgeapps` | `EdgeApps.tsx` | 真实 BFF 数据 | `GET /api/v1/edgeapplication`、`GET /api/v1/edgeapplication/{namespace}`、`GET /api/v1/edgeapplication/{namespace}/{name}`、`POST /api/v1/edgeapplication/{namespace}`、`PUT /api/v1/edgeapplication/{namespace}`、`DELETE /api/v1/edgeapplication/{namespace}/{name}` | 已打通列表 | 补详情字段、创建/更新/删除真实调用 |
| 持久卷 | `/persistentvolumes` | `PersistentVolumes.tsx` | 真实 Kubernetes API 数据 | 官方 BFF 当前未发现 PV 路由；已通过 `api-gateway /storage/persistentvolumes` 增强 | 已打通列表 | 官方 BFF 后续若补路由，可再切回 BFF |
| 持久卷声明 | `/persistentvolumeclaims` | `PersistentVolumeClaims.tsx` | 真实 Kubernetes API 数据 | 官方 BFF 当前未发现 PVC 路由；已通过 `api-gateway /storage/persistentvolumeclaims` 增强 | 已打通列表 | 官方 BFF 后续若补路由，可再切回 BFF |
| 设备模型 | `/devicemodels` | `DeviceModels.tsx` | 真实 BFF 数据 | `GET /api/v1/devicemodel`、`GET /api/v1/devicemodel/{namespace}`、`GET /api/v1/devicemodel/{namespace}/{name}`、`POST /api/v1/devicemodel/{namespace}`、`PUT /api/v1/devicemodel/{namespace}`、`DELETE /api/v1/devicemodel/{namespace}/{name}` | 已打通列表 | 补详情字段、创建/更新/删除真实调用 |
| 设备实例 | `/deviceinstances` | `DeviceInstances.tsx` | 真实 BFF 数据 | `GET /api/v1/device`、`GET /api/v1/device/{namespace}`、`GET /api/v1/device/{namespace}/{name}`、`POST /api/v1/device/{namespace}`、`PUT /api/v1/device/{namespace}`、`DELETE /api/v1/device/{namespace}/{name}` | 已打通列表 | 补详情字段、模型/节点关联、创建/更新/删除真实调用 |
| 规则端点 | `/ruleendpoints` | `RuleEndpoints.tsx` | 真实 BFF 数据 | `GET /api/v1/ruleendpoint`、`GET /api/v1/ruleendpoint/{namespace}`、`GET /api/v1/ruleendpoint/{namespace}/{name}`、`POST /api/v1/ruleendpoint/{namespace}`、`PUT /api/v1/ruleendpoint/{namespace}`、`DELETE /api/v1/ruleendpoint/{namespace}/{name}` | 已打通列表 | 补详情字段、创建/更新/删除真实调用 |
| 规则 | `/rules` | `Rules.tsx` | 真实 BFF 数据 | `GET /api/v1/rule`、`GET /api/v1/rule/{namespace}`、`GET /api/v1/rule/{namespace}/{name}`、`POST /api/v1/rule/{namespace}`、`PUT /api/v1/rule/{namespace}`、`DELETE /api/v1/rule/{namespace}/{name}` | 已打通列表 | 补详情字段、创建/更新/删除真实调用 |
| 服务 | `/services` | `Services.tsx` | 真实 BFF 数据 | `GET /api/v1/service`、`GET /api/v1/service/{namespace}`、`GET /api/v1/service/{namespace}/{name}`、`POST /api/v1/service/{namespace}`、`PUT /api/v1/service/{namespace}`、`DELETE /api/v1/service/{namespace}/{name}` | 已打通列表 | 补详情字段、创建/更新/删除真实调用 |
| 服务账户 | `/serviceaccounts` | `ServiceAccounts.tsx` | 真实 BFF 数据 | `GET /api/v1/serviceaccount`、`GET /api/v1/serviceaccount/{namespace}`、`GET /api/v1/serviceaccount/{namespace}/{name}`、`POST /api/v1/serviceaccount/{namespace}`、`PUT /api/v1/serviceaccount/{namespace}`、`DELETE /api/v1/serviceaccount/{namespace}/{name}` | 已打通列表 | 补详情字段、创建/更新/删除真实调用 |
| 角色 | `/roles` | `Roles.tsx` | 真实 BFF 数据 | `GET /api/v1/role`、`GET /api/v1/role/{namespace}`、`GET /api/v1/role/{namespace}/{name}`、`POST /api/v1/role/{namespace}`、`PUT /api/v1/role/{namespace}`、`DELETE /api/v1/role/{namespace}/{name}` | 已打通列表 | 补详情字段、创建/更新/删除真实调用 |
| 角色绑定 | `/rolebindings` | `RoleBindings.tsx` | 真实 BFF 数据 | `GET /api/v1/rolebinding`、`GET /api/v1/rolebinding/{namespace}`、`GET /api/v1/rolebinding/{namespace}/{name}`、`POST /api/v1/rolebinding/{namespace}`、`PUT /api/v1/rolebinding/{namespace}`、`DELETE /api/v1/rolebinding/{namespace}/{name}` | 已打通列表 | 补详情字段、创建/更新/删除真实调用 |
| 集群角色 | `/clusterroles` | `ClusterRoles.tsx` | 真实 BFF 数据 | `GET /api/v1/clusterrole`、`GET /api/v1/clusterrole/{name}`、`POST /api/v1/clusterrole`、`PUT /api/v1/clusterrole`、`DELETE /api/v1/clusterrole/{name}` | 已打通列表 | 补详情字段、创建/更新/删除真实调用 |
| 集群角色绑定 | `/clusterrolebindings` | `ClusterRoleBindings.tsx` | 真实 BFF 数据 | `GET /api/v1/clusterrolebinding`、`GET /api/v1/clusterrolebinding/{name}`、`POST /api/v1/clusterrolebinding`、`PUT /api/v1/clusterrolebinding`、`DELETE /api/v1/clusterrolebinding/{name}` | 已打通列表 | 补详情字段、创建/更新/删除真实调用 |
| 自定义资源定义 | `/crds` | `CustomResourceDefinitions.tsx` | 真实 BFF 数据 | `GET /api/v1/crd`、`GET /api/v1/crd/{name}` | 已打通列表 | 补详情字段 |

## 3. 官方 BFF 已提供但前端当前没有单独页面的接口

| 接口 | 用途 | 建议 |
| --- | --- | --- |
| `GET /api/v1/namespace` | 命名空间列表 | 已接入 `NamespaceSelector`，用于所有带 namespace 的页面筛选器 |
| `GET /api/v1/pod`、`GET /api/v1/pod/{namespace}` | Pod 列表 | 可用于部署详情、边缘应用详情、节点详情的 Pod 列表 |
| `GET /api/v1/configmap`、`GET /api/v1/configmap/{namespace}`、`GET /api/v1/configmap/{namespace}/{name}` | ConfigMap | 当前前端没有独立页面，可后续扩展 |
| `GET /api/v1/secret`、`GET /api/v1/secret/{namespace}`、`GET /api/v1/secret/{namespace}/{name}` | Secret | 当前前端没有独立页面，可后续扩展 |
| `GET /api/v1/version` | Kubernetes 版本 | 可用于仪表板或系统信息卡片 |

## 4. 需要优先处理的问题

1. 统一 API 前缀

当前官方 BFF 是 `/api/v1`。前端运行时已经按 `/api/v1` 启动，但代码默认值和文档中还有 `/api` 的旧描述，建议统一调整。

涉及文件：

```text
frontend/src/api/request.ts
frontend/.env.example
server/api-gateway/.env.example
docs/api-contract.md
README.md
代码结构梳理.md
```

2. 替换 mock 数据

页面列表数据已不再直接依赖：

```text
frontend/src/data/mockData.ts
```

目前已完成列表数据替换；PV/PVC 因官方 BFF 缺路由，暂时走 `api-gateway` 增强层直连 Kubernetes 原生 API；创建表单里的命名空间选项也已改为真实 namespace。

```text
frontend/src/api/services/resources.ts
frontend/src/api/adapters/*.adapter.ts
```

3. 增强 adapter

官方 BFF 返回结构和 Kubernetes 原始对象不完全一致。例如节点接口返回的是简化对象：

```json
{
  "name": "k8s-master",
  "status": "Ready",
  "internalIP": "192.168.16.52",
  "kubeletVersion": "v1.28.15"
}
```

所以 adapter 需要兼容两类数据：

```text
官方 BFF 简化对象
Kubernetes 原始对象
```

4. 梳理写操作

当前很多页面有创建、更新、删除按钮，但前端多数还只是本地状态变更。接真实接口时需要逐个确认：

```text
POST / PUT / DELETE 是否符合官方 BFF 请求体
删除操作是否需要二次确认
创建表单字段是否足够生成合法 Kubernetes / KubeEdge 资源
```

5. 明确 BFF 缺口

前端已有 PV/PVC 页面，但当前官方 BFF 未发现对应路由：

```text
PersistentVolume
PersistentVolumeClaim
```

这两类页面需要确认产品上是否保留。如果保留，需要通过 `server/api-gateway` 增强层补接口，或者后续扩展官方 BFF。

## 5. 建议开发顺序

| 优先级 | 页面/模块 | 原因 |
| --- | --- | --- |
| P0 | API 前缀、token、本地启动脚本 | 保证开发联调稳定 |
| P0 | NamespaceSelector | 已接真实 namespace，后续补加载/错误细节即可 |
| P1 | 节点 | 已经打通，可补详情/操作，作为标准样板 |
| P1 | 部署、服务、Pod | Kubernetes 核心资源，官方 BFF 完整支持 |
| P1 | 设备模型、设备实例 | KubeEdge 核心资源 |
| P1 | 规则端点、规则 | KubeEdge 数据流核心资源 |
| P2 | RBAC 相关页面 | 列表已接入，继续补写操作和详情字段 |
| P2 | CRD | 列表已接入，后续补详情字段 |
| P3 | PV/PVC | 官方 BFF 暂无路由，当前已由 api-gateway 先补只读接口 |
| P3 | ConfigMap/Secret | 当前前端无页面，可按产品需要扩展 |

## 6. 当前可以向 leader 汇报的状态

```text
1. 架构链路已验证通过：前端 -> 官方 BFF -> 真实集群。
2. 官方 BFF 实际接口前缀为 /api/v1，已确认和早期文档存在差异。
3. 节点、节点组、部署、边缘应用、服务、设备模型、设备实例、规则端点、规则、服务账户、角色、角色绑定、集群角色、集群角色绑定、CRD 页面已接入真实 BFF 列表数据。
4. 仪表盘已接入节点/部署概览，CPU/内存趋势和事件仍需 metrics 或事件接口。
5. PV/PVC 暂未在官方 BFF 发现接口，已先通过 api-gateway 增强层补只读真实数据。
6. 官方 BFF 对 Kubernetes/KubeEdge 核心资源支持较完整，适合后续前端持续开发。
```
