# BlueEdge 页面与官方 BFF 对接梳理

更新时间：2026-05-28

## 1. 当前结论

当前主链路已经跑通，并且前端页面已从 mock 列表切到真实接口列表数据：

```text
frontend -> Vite proxy -> official BFF /api/v1 -> Kubernetes / KubeEdge cluster
frontend -> Vite proxy -> api-gateway /product-api -> official BFF /api/v1 或 Kubernetes API
```

接口前缀已统一为：

```text
/api/v1
```

相关默认配置也已同步：

```text
frontend/src/api/request.ts                VITE_BFF_BASE_URL 默认 /api/v1
frontend/.env.example                      VITE_BFF_BASE_URL=/api/v1
server/api-gateway/.env.example            BFF_BASE_URL=http://127.0.0.1:8080/api/v1
```

已验证过节点接口可从真实集群返回数据：

```text
GET /api/v1/node
```

当前真实节点数据包括：

```text
k8s-laptop-edge
k8s-master
k8s-worker01
```

## 2. 页面对接状态总表

| 模块 | 前端路由 | 前端页面 | 当前数据来源 | 已接接口 | 当前状态 | 主要待办 |
| --- | --- | --- | --- | --- | --- | --- |
| 仪表板 | `/` | `Dashboard.tsx` | 真实 BFF + api-gateway metrics | `GET /api/v1/node`、`GET /api/v1/deployment`、`GET /product-api/metrics/cluster` | 已接节点/部署概览与集群 CPU/内存当前值 | 趋势图目前用当前值铺平，事件流仍需事件接口 |
| 节点 | `/nodes` | `Nodes.tsx` | 真实 BFF + api-gateway metrics | `GET /api/v1/node`、`GET /api/v1/node/{name}`、`PUT /api/v1/node`、`DELETE /api/v1/node/{name}`、`GET /product-api/metrics/nodes` | 已接列表、详情补拉、节点 CPU/内存指标、真实删除、调度开关 | Node 无创建路由；添加节点需由 kubelet/edgecore 注册，页面仅提示不支持创建 |
| 节点组 | `/nodegroups` | `NodeGroups.tsx` | 真实 BFF 数据 | `GET /api/v1/nodegroup`、`GET /api/v1/nodegroup/{name}`、`POST /api/v1/nodegroup`、`DELETE /api/v1/nodegroup/{name}`、`GET /api/v1/node` | 已接列表、详情补拉、节点关联、真实创建、真实删除 | 补真实更新/编辑，并按集群 CRD 校验 NodeGroup spec 字段 |
| 部署 | `/deployments` | `Deployments.tsx` | 真实 BFF + api-gateway metrics | `GET /api/v1/deployment`、`GET /api/v1/deployment/{namespace}`、`GET /api/v1/deployment/{namespace}/{name}`、`POST /api/v1/deployment/{namespace}`、`PUT /api/v1/deployment/{namespace}`、`DELETE /api/v1/deployment/{namespace}/{name}`、`GET /product-api/workloads/pods`、`GET /product-api/metrics/pods` | 已接列表、详情补拉、Pod 关联、所在节点、Pod 指标、真实创建/删除/扩缩容/暂停 | 继续补事件、日志、表单校验和更多更新字段 |
| 边缘应用 | `/edgeapps` | `EdgeApps.tsx` | 真实 BFF 数据 | `GET /api/v1/edgeapplication`、`GET /api/v1/edgeapplication/{namespace}`、`POST /api/v1/edgeapplication/{namespace}`、`DELETE /api/v1/edgeapplication/{namespace}/{name}` | 已接列表、真实创建、真实删除；重启/暂停已停止本地假状态 | 补真实更新/详情，并确认 EdgeApplication workloadTemplate、暂停/重启语义 |
| 持久卷 | `/persistentvolumes` | `PersistentVolumes.tsx` | 真实 Kubernetes API 数据 | `GET /product-api/storage/persistentvolumes` | 已接只读列表 | 官方 BFF 暂无 PV 路由；创建/删除仍是本地状态 |
| 持久卷声明 | `/persistentvolumeclaims` | `PersistentVolumeClaims.tsx` | 真实 Kubernetes API 数据 | `GET /product-api/storage/persistentvolumeclaims` | 已接只读列表，支持 namespace 查询 | 官方 BFF 暂无 PVC 路由；创建/删除仍是本地状态 |
| 设备模型 | `/devicemodels` | `DeviceModels.tsx` | 真实 BFF 数据 | `GET /api/v1/devicemodel`、`GET /api/v1/devicemodel/{namespace}`、`POST /api/v1/devicemodel/{namespace}`、`DELETE /api/v1/devicemodel/{namespace}/{name}` | 已接列表、真实创建、真实删除 | 补真实更新/详情、属性编辑和表单校验 |
| 设备实例 | `/deviceinstances` | `DeviceInstances.tsx` | 真实 BFF 数据 | `GET /api/v1/device`、`GET /api/v1/device/{namespace}`、`POST /api/v1/device/{namespace}`、`DELETE /api/v1/device/{namespace}/{name}` | 已接列表、真实创建、真实删除 | 补真实更新/详情、模型/节点选项关联和表单校验 |
| 规则端点 | `/ruleendpoints` | `RuleEndpoints.tsx` | 真实 BFF 数据 | `GET /api/v1/ruleendpoint`、`GET /api/v1/ruleendpoint/{namespace}`、`POST /api/v1/ruleendpoint/{namespace}`、`DELETE /api/v1/ruleendpoint/{namespace}/{name}` | 已接列表、真实创建、真实删除 | 补真实更新/详情、properties 编辑和表单校验 |
| 规则 | `/rules` | `Rules.tsx` | 真实 BFF 数据 | `GET /api/v1/rule`、`GET /api/v1/rule/{namespace}`、`POST /api/v1/rule/{namespace}`、`DELETE /api/v1/rule/{namespace}/{name}`、`GET /api/v1/ruleendpoint` | 已接列表、真实创建、真实删除，并拉取 ruleendpoint 用于关联名称 | 补真实更新/详情、sourceResource/targetResource 编辑和表单校验 |
| 服务 | `/services` | `Services.tsx` | 真实 BFF 数据 | `GET /api/v1/service`、`GET /api/v1/service/{namespace}`、`POST /api/v1/service/{namespace}`、`DELETE /api/v1/service/{namespace}/{name}` | 已接列表、真实创建、真实删除 | 补详情字段、真实更新、NodePort/LoadBalancer 表单校验 |
| 服务账户 | `/serviceaccounts` | `ServiceAccounts.tsx` | 真实 BFF 数据 | `GET /api/v1/serviceaccount`、`GET /api/v1/serviceaccount/{namespace}`、`POST /api/v1/serviceaccount/{namespace}`、`PUT /api/v1/serviceaccount/{namespace}`、`DELETE /api/v1/serviceaccount/{namespace}/{name}` | 已接列表、真实创建、真实编辑、真实删除 | 补更完整详情字段和表单校验 |
| 角色 | `/roles` | `Roles.tsx` | 真实 BFF 数据 | `GET /api/v1/role`、`GET /api/v1/role/{namespace}`、`POST /api/v1/role/{namespace}`、`PUT /api/v1/role/{namespace}`、`DELETE /api/v1/role/{namespace}/{name}` | 已接列表、真实创建、真实编辑、真实删除 | 补多规则编辑、更完整详情字段和表单校验 |
| 角色绑定 | `/rolebindings` | `RoleBindings.tsx` | 真实 BFF 数据 | `GET /api/v1/rolebinding`、`GET /api/v1/rolebinding/{namespace}`、`POST /api/v1/rolebinding/{namespace}`、`PUT /api/v1/rolebinding/{namespace}`、`DELETE /api/v1/rolebinding/{namespace}/{name}` | 已接列表、真实创建、真实编辑、真实删除 | 补主体类型选择、多主体编辑和更完整详情字段 |
| 集群角色 | `/clusterroles` | `ClusterRoles.tsx` | 真实 BFF 数据 | `GET /api/v1/clusterrole`、`POST /api/v1/clusterrole`、`PUT /api/v1/clusterrole`、`DELETE /api/v1/clusterrole/{name}` | 已接列表、真实创建、真实编辑、真实删除 | 补多规则编辑、更完整详情字段和表单校验 |
| 集群角色绑定 | `/clusterrolebindings` | `ClusterRoleBindings.tsx` | 真实 BFF 数据 | `GET /api/v1/clusterrolebinding`、`POST /api/v1/clusterrolebinding`、`PUT /api/v1/clusterrolebinding`、`DELETE /api/v1/clusterrolebinding/{name}` | 已接列表、真实创建、真实编辑、真实删除 | 补主体类型选择、多主体编辑和更完整详情字段 |
| 自定义资源定义 | `/crds` | `CustomResourceDefinitions.tsx` | 真实 BFF 数据 | `GET /api/v1/crd` | 已接列表 | 补详情字段 |

## 3. 已接入的前端接口层

| 文件 | 职责 | 当前说明 |
| --- | --- | --- |
| `frontend/src/api/request.ts` | 统一请求封装 | 负责 BFF 与 api-gateway 的 base URL、token、query params、错误处理 |
| `frontend/src/api/services/resources.ts` | 官方 BFF 资源服务 | 已覆盖 node、namespace、deployment、pod、device、devicemodel、service、rule、ruleendpoint、nodegroup、edgeapplication、RBAC、CRD；已新增通用 create/update/delete 写操作封装和各资源具名写方法 |
| `frontend/src/api/services/storage.ts` | 存储增强接口 | PV/PVC 通过 api-gateway 直连 Kubernetes API |
| `frontend/src/api/services/metrics.ts` | 指标增强接口 | 节点指标、Pod 指标通过 metrics.k8s.io 获取 |
| `frontend/src/api/services/product.ts` | 产品聚合接口 | Dashboard 使用集群 metrics；`/overview` 仍保留为产品聚合接口 |
| `frontend/src/api/adapters/*.adapter.ts` | 页面数据适配 | 已覆盖 node、deployment、device、devicemodel、service、rule、ruleendpoint、通用 kube resource |

## 4. api-gateway 当前增强接口

| 接口 | 数据来源 | 用途 |
| --- | --- | --- |
| `GET /product-api/healthz` | api-gateway 本身 | 健康检查 |
| `GET /product-api/overview` | 官方 BFF `/node`、`/deployment`、`/device`、`/rule` | 产品聚合概览，当前 Dashboard 未直接使用但可保留 |
| `GET /product-api/storage/persistentvolumes` | Kubernetes `/api/v1/persistentvolumes` | PV 列表 |
| `GET /product-api/storage/persistentvolumeclaims?namespace=` | Kubernetes `/api/v1/persistentvolumeclaims` 或命名空间 PVC | PVC 列表 |
| `GET /product-api/metrics/cluster` | Kubernetes `metrics.k8s.io/v1beta1/nodes` + node capacity | Dashboard 集群 CPU/内存当前值 |
| `GET /product-api/metrics/nodes` | Kubernetes `metrics.k8s.io/v1beta1/nodes` + node capacity | 节点列表 CPU/内存使用率 |
| `GET /product-api/metrics/pods?namespace=` | Kubernetes `metrics.k8s.io/v1beta1/pods` | Deployment 详情中的 Pod CPU/内存 |
| `GET /product-api/workloads/pods?namespace=` | Kubernetes `/api/v1/pods` 或命名空间 Pod | Deployment 列表与详情聚合 Pod 所在节点，兜底对齐 `kubectl get pods -o wide` |
| `/product-api/bff/*` | 官方 BFF 透传 | 预留 BFF 代理入口 |

## 5. 官方 BFF 已提供但前端当前没有单独页面的接口

| 接口 | 用途 | 建议 |
| --- | --- | --- |
| `GET /api/v1/namespace` | 命名空间列表 | 已接入 `NamespaceSelector`，用于所有带 namespace 的页面筛选器 |
| `GET /api/v1/pod`、`GET /api/v1/pod/{namespace}` | Pod 列表 | 已用于 Deployment 详情，可继续用于边缘应用详情、节点详情 |
| `GET /api/v1/configmap`、`GET /api/v1/configmap/{namespace}`、`GET /api/v1/configmap/{namespace}/{name}` | ConfigMap | 当前前端没有独立页面，可后续扩展 |
| `GET /api/v1/secret`、`GET /api/v1/secret/{namespace}`、`GET /api/v1/secret/{namespace}/{name}` | Secret | 当前前端没有独立页面，可后续扩展 |
| `GET /api/v1/version` | Kubernetes 版本 | 可用于仪表板或系统信息卡片 |

## 6. 需要优先处理的问题

1. 写操作需要继续逐页落地

当前已补通用写操作 service 封装，并优先接入部署、服务、节点：

```text
Deployment: POST / PUT / DELETE 已接，创建/删除/扩缩容/暂停成功后重新拉列表
Service: POST / DELETE 已接，成功后重新拉列表
Node: PUT / DELETE 已接，支持调度开关和删除；Node 无 POST 创建路由
```

KubeEdge 资源已按页面补到真实创建/删除：设备模型、设备实例、规则端点、规则、节点组、边缘应用。RBAC 已补真实编辑/更新。后续继续补 KubeEdge 资源更新/详情、表单校验，以及 RBAC 多规则/多主体编辑。

2. metrics 可用性需要在部署侧兜底

CPU/内存相关接口依赖集群安装 metrics-server，并要求 `K8S_API_SERVER`、`K8S_SKIP_TLS_VERIFY`、token 权限正确。当前前端已经能展示 metrics 调用失败的降级状态，后续要在部署文档中明确依赖。

3. Dashboard 趋势和事件仍不是完整真实数据

Dashboard 已接入 `/product-api/metrics/cluster`，但趋势图目前是用当前值生成的静态序列；事件列表仍是页面内静态展示，需要补 Kubernetes events 或产品事件接口。

4. PV/PVC 仍属于 BFF 缺口

官方 BFF 当前未发现 PV/PVC 路由，已通过 `server/api-gateway` 直连 Kubernetes API 补只读列表。后续如果官方 BFF 增加存储路由，可考虑把前端 `storage.ts` 切回 BFF 或保留 gateway 聚合。

5. adapter 还需要持续收敛

官方 BFF 返回结构和 Kubernetes 原始对象不完全一致，adapter 需要继续兼容两类数据：

```text
官方 BFF 简化对象
Kubernetes 原始对象
```

已经有独立 adapter 的资源优先在 adapter 层补字段，不建议在页面里直接散落兼容逻辑。

## 7. 建议开发顺序

| 优先级 | 页面/模块 | 原因 |
| --- | --- | --- |
| P0 | 写操作 service 封装 | 列表已接真实数据，下一步最影响可用性的是创建/更新/删除真实调用 |
| P0 | 节点、部署 | 部署已接真实创建/删除/扩缩容/暂停；节点已接真实详情、删除、调度开关，可作为读写闭环样板继续完善 |
| P1 | 服务、RBAC | 服务已接真实创建/删除；RBAC 已接真实创建/编辑/删除，继续补多规则/多主体编辑、详情补拉和表单能力 |
| P1 | 设备模型、设备实例 | 已接真实创建/删除；继续补更新/详情、模型/节点关联和表单校验 |
| P1 | 规则端点、规则 | 已接真实创建/删除；继续补更新/详情、properties 与 source/target 资源编辑 |
| P2 | 边缘应用、节点组 | 已接真实创建/删除；继续校验 CRD spec、补更新/详情和状态类操作 |
| P2 | Dashboard 事件/趋势 | metrics 当前值已接，后续补真实时间序列和事件 |
| P3 | PV/PVC | 当前 gateway 只读可用；写操作和 BFF 归属后续再定 |
| P3 | ConfigMap/Secret | 当前前端无页面，可按产品需要扩展 |

## 8. 当前可以向 leader 汇报的状态

```text
1. 架构链路已验证通过：前端 -> 官方 BFF -> 真实集群，增强接口通过 api-gateway 访问 Kubernetes API。
2. 官方 BFF 实际接口前缀为 /api/v1，前端和 gateway 默认配置已统一。
3. 节点、节点组、部署、边缘应用、服务、设备模型、设备实例、规则端点、规则、服务账户、角色、角色绑定、集群角色、集群角色绑定、CRD 页面已接入真实 BFF 列表数据。
4. Dashboard 已接入真实节点/部署数据和集群 CPU/内存当前值；节点页、部署页也已接入 metrics 数据。
5. PV/PVC 暂未在官方 BFF 发现接口，已先通过 api-gateway 增强层补只读真实数据。
6. 写操作 service 封装已完成，部署、服务、节点、RBAC 与 KubeEdge 资源已开始接真实写接口；KubeEdge 资源更新编辑和 PV/PVC 写操作仍需继续补齐。
```

## 9. 最近变更记录

- 2026-05-28: 新增 `frontend/src/api/services/resources.ts` 通用写操作封装，覆盖 namespaced 与 cluster 级资源。
- 2026-05-28: 部署页接入真实创建、删除、扩缩容、暂停/恢复，并通过 Pod 列表聚合展示所在节点。
- 2026-05-28: 服务页接入真实创建、删除。
- 2026-05-28: 节点页接入真实详情补拉、删除、设为不可调度/恢复调度；页面创建节点改为提示不支持，因为官方 BFF 无 `POST /node`。
- 2026-05-28: api-gateway 增加 `/workloads/pods`，并配置 `K8S_API_SERVER=https://127.0.0.1:16443` 用于直连 Kubernetes API。
- 2026-05-28: 服务账户、角色、角色绑定、集群角色、集群角色绑定接入真实创建和删除；操作成功后重新拉列表，避免本地状态与集群不一致。
- 2026-05-28: 设备模型、设备实例、规则端点、规则、节点组、边缘应用接入真实创建和删除；边缘应用重启/暂停停止本地假状态，改为提示未启用真实写入。
- 2026-05-28: 服务账户、角色、角色绑定、集群角色、集群角色绑定补真实编辑/更新；更新成功后重新拉列表，编辑对象保留原始 metadata 以兼容 BFF/Kubernetes 更新要求。
