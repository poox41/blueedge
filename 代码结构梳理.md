
# 代码结构梳理

# 1. 根目录整体含义

当前工程可以理解成四大块：

```text
blueedge/
  frontend/              # BlueEdge 自研前端
  server/api-gateway/    # BlueEdge 自研增强网关
  scripts/               # 初始化、启动、同步、测试脚本
  docs/                  # 架构说明、接口契约、开发计划
  upstream/              # 后续放官方 kubeedge/dashboard submodule
  README.md              # 项目总说明
```

它对应的架构是：

```text
frontend 自研前端
  ↓
src/api/adapters 接口适配层
  ↓
server/api-gateway 产品增强层，可选
  ↓
upstream/kubeedge-dashboard/modules/api 官方 BFF
  ↓
Kubernetes API Server / KubeEdge CRD
```

这个架构在 README 里已经写清楚了：前端完全自研，BFF 复用官方 `kubeedge/dashboard/modules/api`，增强能力放在 `server/api-gateway`，页面不直接消费 BFF 原始返回，而是经过 `frontend/src/api/adapters`。


# 2. README.md：项目总说明书

`README.md` 是整个项目的入口说明。

它主要说明几件事：

```text
1. BlueEdge 是什么
2. 整体架构怎么设计
3. 每个目录大概做什么
4. 第一次怎么启动
5. 如何初始化官方 BFF
6. 如何启动前端、api-gateway、BFF
7. 第一阶段重点接哪些接口
```

你可以把它理解成：

> 给新同事看的第一份说明书。

里面已经明确了当前第一阶段要先打通这些接口：

```text
/node
/deployment
/service
/devicemodel
/device
/ruleendpoint
/rule
```

这说明当前代码的目标不是马上做全量功能，而是先把 KubeEdge 核心资源链路跑通。

---

# 3. docs/：项目文档目录

## 3.1 docs/development-plan.md

这个文件是开发路线图。

它把 BlueEdge 分成几个阶段：

```text
P0：工程链路
P1：核心页面真实数据化
P2：产品化增强
P3：BlueEdge 差异化能力
```

其中 P0 的目标是证明自研前端可以稳定复用官方 KubeEdge Dashboard BFF，包括引入 submodule、跑通 `modules/api`、跑通前端、跑通 api-gateway、完成 token 传递和核心资源适配。

这个文件的作用是：

> 防止开发跑偏，明确每个阶段做什么、不做什么。

现在你最应该盯的是 P0 和 P1。

---

## 3.2 docs/api-contract.md

这个文件非常重要，它是 **前端和 BFF 之间的接口契约**。

它说明：

```text
前端默认请求 /api/v1
Vite 开发环境代理到 http://127.0.0.1:8080/api/v1
第一阶段接口包括 /api/v1/node、/api/v1/deployment/{namespace}、/api/v1/device/{namespace}、/api/v1/devicemodel/{namespace}、/api/v1/rule/{namespace} 等
```

同时它规定了一个核心原则：

> 页面不直接使用 BFF 原始数据，必须转换为 ViewModel。

例如：

```ts
const res = await bffRequest('/node')
const nodes = normalizeNodeList(res.data)
```

也就是说，页面不要直接写 Kubernetes 原始字段，而是通过 adapter 转换。这样后面官方 BFF 返回结构变化时，优先改 adapter，不改页面。

这个文件相当于：

> 前后端联调边界说明书。

---

# 4. scripts/：工程脚本目录

`scripts/` 里面是项目初始化和启动用的脚本。

## 4.1 scripts/init-upstream.sh

这个脚本的作用是：

> 把官方 `kubeedge/dashboard` 仓库作为 submodule 引入到当前项目里。

脚本里执行的是：

```bash
git submodule add https://github.com/kubeedge/dashboard.git upstream/kubeedge-dashboard
```

引入后，官方代码会放在：

```text
upstream/kubeedge-dashboard
```

我们后续主要使用：

```text
upstream/kubeedge-dashboard/modules/api
```

注意，这个脚本只是帮你初始化 submodule。真正完成后还需要提交：

```bash
git add .gitmodules upstream/kubeedge-dashboard
git commit -m "chore: add kubeedge dashboard upstream"
```

脚本里也明确提示了这一步。

---

## 4.2 scripts/run-bff.sh

这个脚本的作用是：

> 启动官方 KubeEdge Dashboard 的 BFF，也就是 `modules/api`。

它要求你先设置：

```bash
export KUBE_APISERVER=https://你的-apiserver:6443
export APISERVER_SKIP_TLS_VERIFY=true
```

然后它会进入：

```text
upstream/kubeedge-dashboard/modules/api
```

执行：

```bash
go run main.go --apiserver-host="$KUBE_APISERVER" --apiserver-skip-tls-verify="$SKIP_TLS"
```

也就是说，这个脚本负责把官方 BFF 跑起来，让前端可以请求：

```text
http://127.0.0.1:8080/api/v1
```

这个是当前项目最关键的真实联调入口。

---

## 4.3 scripts/sync-upstream.sh

这个脚本的设计目的应该是：

> 后续同步官方 `kubeedge/dashboard` 更新。

也就是当官方 Dashboard 更新后，你可以进入 submodule 拉最新代码，然后再跑接口契约测试。

它对应我们之前定的同步机制：

```text
更新官方 upstream
  ↓
重新启动 BFF
  ↓
跑 contract test
  ↓
如果接口变了，改 adapter
```

---

## 4.4 scripts/run-frontend.sh

这个脚本的作用是：

> 启动 BlueEdge 自研前端。

本质上就是进入 `frontend/`，执行：

```bash
npm install
npm run dev
```

前端默认跑在：

```text
http://127.0.0.1:3000
```

---

## 4.5 scripts/run-gateway.sh

这个脚本的作用是：

> 启动 BlueEdge 自己的 `api-gateway` 增强层。

注意：`api-gateway` 不是官方 BFF，它是我们自己的产品增强层。

后面首页统计、云边拓扑、边缘节点健康评分这些能力，不建议塞进官方 BFF，而是放在这里。

---

## 4.6 scripts/contract-test.sh

这个脚本的作用是：

> 做接口契约冒烟测试。

简单说，就是每次官方 Dashboard BFF 更新后，用它快速检查：

```text
/node 能不能通
/deployment 能不能通
/device 能不能通
/devicemodel 能不能通
/rule 能不能通
```

它不负责测完整业务，只负责确认：

```text
接口路径还在
状态码正常
token 还能传
返回结构 adapter 还能解析
```

---

# 5. frontend/：自研前端目录

这是 BlueEdge 的核心产品前端。

当前 `frontend/package.json` 说明它是一个独立的 Vite + React + TypeScript 工程，依赖里有 React、Vite、Tailwind、Radix/shadcn、Recharts、React Router 等。

也就是说：

> 这个前端不是直接复用官方 Dashboard 前端，而是我们自己的产品化 UI 工程。

---

# 6. frontend/src/api/：前端接口层

这是当前代码最关键的目录。

它负责把页面和后端隔离开。

大概结构是：

```text
frontend/src/api/
  request.ts
  services/
    resources.ts
    product.ts
  adapters/
    common.ts
    node.adapter.ts
    deployment.adapter.ts
    device.adapter.ts
    device-model.adapter.ts
    rule.adapter.ts
```

---

## 6.1 frontend/src/api/request.ts

这是统一请求封装。

它做几件事：

```text
1. 定义 HTTP 请求方法
2. 定义请求参数结构
3. 读取 token
4. 拼接请求 URL
5. 统一处理 response
6. 提供 bffRequest
7. 提供 gatewayRequest
```

里面有两个核心 baseURL：

```ts
const BFF_BASE_URL = import.meta.env.VITE_BFF_BASE_URL || "/api/v1";
const GATEWAY_BASE_URL = import.meta.env.VITE_GATEWAY_BASE_URL || "/product-api";
```

也就是说：

```text
bffRequest      → 请求官方 KubeEdge Dashboard BFF
gatewayRequest  → 请求 BlueEdge 自研 api-gateway
```

它还会从 localStorage 读取：

```text
token
blueedge.token
```

然后自动加到请求头里：

```text
Authorization: Bearer <token>
```

所以这个文件是：

> 所有前端 API 请求的统一入口。

---

## 6.2 frontend/src/api/services/resources.ts

这个文件是资源 API 服务层。

它已经封装了这些函数：

```ts
listNodes()
listDeployments(namespace?)
listDeviceModels(namespace?)
listDevices(namespace?)
listRules(namespace?)
```

比如：

```ts
export async function listNodes(): Promise<EdgeNodeView[]> {
  const res = await bffRequest<unknown>("/node");
  return normalizeNodeList(res.data);
}
```

这个逻辑非常关键：

```text
页面
  ↓
调用 listNodes()
  ↓
bffRequest('/node')
  ↓
官方 BFF
  ↓
返回原始数据
  ↓
normalizeNodeList()
  ↓
页面拿到 EdgeNodeView[]
```

所以 `resources.ts` 是：

> 页面调用后端资源的服务入口。

---

## 6.3 frontend/src/api/adapters/common.ts

这个文件是所有 adapter 的公共工具。

它解决几个通用问题：

### 第一，兼容不同返回结构

官方 BFF 返回可能是：

```ts
payload.items
```

也可能是：

```ts
payload.data.items
```

也可能是：

```ts
payload.data
```

所以 `asItems()` 会统一处理，把各种结构都转成数组：

```ts
export function asItems<T = KubeResource>(payload: unknown): T[]
```

### 第二，统一取 metadata 字段

比如：

```ts
getName()
getNamespace()
getCreatedAt()
```

### 第三，安全读取嵌套字段

比如：

```ts
getNestedString(raw, ["status", "nodeInfo", "osImage"])
```

这样可以避免字段不存在时报错。

这个文件的作用是：

> 给所有资源 adapter 提供通用解析方法。

---

## 6.4 frontend/src/api/adapters/node.adapter.ts

这个文件专门处理 Node 资源。

它把 Kubernetes Node 原始对象转成前端页面好用的结构：

```ts
EdgeNodeView
```

它做了几件事：

```text
1. 从 status.conditions 判断 Ready / NotReady / Unknown
2. 从 status.addresses 里取 InternalIP
3. 根据 labels 判断是 cloud node 还是 edge node
4. 读取 osImage
5. 读取 kubeletVersion
6. 保留 raw 原始对象，方便后续 YAML 查看
```

核心转换结果类似：

```ts
{
  name: "edge-node-01",
  status: "Ready",
  role: "edge",
  internalIP: "192.168.1.10",
  osImage: "Ubuntu 22.04",
  kubeletVersion: "v1.28.x",
  createdAt: "...",
  raw: 原始K8s对象
}
```

这个文件就是：

> 把后端返回的 K8s Node 原始数据，转换成 BlueEdge 节点页面使用的数据结构。

---

## 6.5 deployment.adapter.ts

这个文件的作用应该是处理 Deployment。

它一般会把原始 Deployment 转成：

```ts
WorkloadView
```

包括：

```text
name
namespace
replicas
availableReplicas
updatedReplicas
ready
createdAt
raw
```

对应页面就是后续的：

```text
工作负载 / 边缘应用 / Deployment 列表
```

---

## 6.6 device-model.adapter.ts

这个文件处理 KubeEdge 的 DeviceModel。

它一般会把设备模型转成：

```ts
DeviceModelView
```

字段包括：

```text
name
namespace
propertiesCount
createdAt
raw
```

这个页面后续对应：

```text
设备管理 → 设备模型
```

---

## 6.7 device.adapter.ts

这个文件处理 KubeEdge 的 Device 实例。

它一般会转成：

```ts
DeviceView
```

字段包括：

```text
name
namespace
model
nodeName
status
createdAt
raw
```

这个页面后续对应：

```text
设备管理 → 设备实例
```

---

## 6.8 rule.adapter.ts

这个文件处理 KubeEdge Rule。

它一般会转成：

```ts
RuleView
```

字段包括：

```text
name
namespace
source
target
createdAt
raw
```

这个页面后续对应：

```text
规则流转 → 规则配置
```

---

# 7. frontend/src/types/kubeedge.ts：统一类型定义

这个文件定义了 BlueEdge 前端内部使用的类型。

主要分两类。

## 第一类：Kubernetes 原始资源基础类型

```ts
KubeObjectMeta
KubeList<T>
KubeResource
```

这些是为了兼容 Kubernetes / KubeEdge 返回的原始对象，比如：

```text
metadata
spec
status
labels
annotations
creationTimestamp
```

## 第二类：页面使用的 ViewModel

比如：

```ts
EdgeNodeView
WorkloadView
DeviceModelView
DeviceView
RuleView
```

这些才是页面真正应该消费的数据结构。

比如 `EdgeNodeView` 包括：

```text
name
status
role
internalIP
osImage
kubeletVersion
createdAt
raw
```

`DeviceView` 包括：

```text
name
namespace
model
nodeName
status
createdAt
raw
```

这个文件的意义是：

> 统一规定页面到底使用什么数据结构，避免页面直接依赖 Kubernetes 原始对象。

---

# 8. server/api-gateway/：产品增强层

这个目录是为 BlueEdge 自己的增强能力预留的。

它不等于官方 BFF。

官方 BFF 负责：

```text
访问 Kubernetes / KubeEdge 资源
```

api-gateway 负责：

```text
做产品聚合和增强
```

比如后续首页要展示：

```text
边缘节点数量
在线设备数量
异常设备数量
边缘应用数量
规则数量
云边健康状态
```

这些数据可能来自多个 BFF 接口：

```text
/node
/device
/deployment
/rule
```

如果都让前端页面自己聚合，会比较乱。

所以 api-gateway 可以提供：

```text
GET /product-api/overview
```

然后它内部去调用多个官方 BFF 接口，再返回一个首页需要的聚合结构。

这个目录后续会越来越重要。

---

# 9. upstream/：官方 Dashboard 代码目录

这个目录现在应该用来放官方：

```text
kubeedge/dashboard
```

通过 submodule 引入后，结构会类似：

```text
upstream/kubeedge-dashboard/
  modules/
    api/
    common/
    web/
```

我们真正复用的是：

```text
upstream/kubeedge-dashboard/modules/api
```

它就是官方 BFF。

注意：

> `upstream/` 不是我们主要写业务代码的地方。
> 尽量不要直接魔改里面的官方代码。

如果必须增强能力，优先放到：

```text
server/api-gateway/
```

---

# 10. 整体调用链路

以后一个页面请求节点数据，大概是这样：

```text
Nodes 页面
  ↓
调用 listNodes()
  ↓
frontend/src/api/services/resources.ts
  ↓
bffRequest('/node')
  ↓
frontend/src/api/request.ts
  ↓
官方 BFF：upstream/kubeedge-dashboard/modules/api
  ↓
Kubernetes API Server
  ↓
返回 Node 原始数据
  ↓
normalizeNodeList()
  ↓
frontend/src/api/adapters/node.adapter.ts
  ↓
返回 EdgeNodeView[]
  ↓
页面展示
```

这就是整个项目最核心的思想。

---

# 11. 当前这份代码最重要的几个文件

你现在最应该关注这几个：

```text
README.md
docs/development-plan.md
docs/api-contract.md

scripts/init-upstream.sh
scripts/run-bff.sh
scripts/contract-test.sh

frontend/src/api/request.ts
frontend/src/api/services/resources.ts
frontend/src/api/adapters/common.ts
frontend/src/api/adapters/node.adapter.ts
frontend/src/types/kubeedge.ts
```

其中最核心的是：

```text
request.ts
resources.ts
adapters/
types/kubeedge.ts
```

因为它们决定了前端如何优雅地接官方 BFF。

---

# 12. 你可以这样理解当前代码状态

当前代码分三层成熟度：

## 已经比较明确的

```text
工程目录
架构原则
启动脚本
request 封装
adapter 规范
核心类型定义
第一阶段接口契约
```

## 已经有雏形但需要真实联调的

```text
node adapter
deployment adapter
device adapter
deviceModel adapter
rule adapter
resources service
BFF 启动脚本
contract test
```

## 后续还要继续写的

```text
真实页面接 API
首页 overview 聚合
设备页面真实数据化
规则流转页面真实数据化
YAML 查看器
详情抽屉
错误/空状态/loading
api-gateway 真实聚合逻辑
```

---
